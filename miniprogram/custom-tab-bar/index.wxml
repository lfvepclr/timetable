<!-- custom-tab-bar/index.wxml -->
<t-tab-bar value="{{selected}}" bind:change="switchTab" class="custom-tab-bar" t-class="tab-bar-root" safe-area-inset-bottom>
  <t-tab-bar-item wx:for="{{tabs}}" wx:key="index" value="{{index}}" icon="{{item.icon}}">{{item.text}}</t-tab-bar-item>
</t-tab-bar>
